package by.tc.task01.dao.properties;

import java.io.File;

public final class Property {
// конструктор у этого класса должен быть private
    public static final String path = "src" + File.separator + "main" + File.separator + "resources" + File.separator + "appliances_db.txt";
// это в каком такой src ты фалы кладешь???? как же ты приложение вне ide собрался запускать?
}
